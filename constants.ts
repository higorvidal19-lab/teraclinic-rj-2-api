import { User, UserRole, Permission, Patient, Appointment, AppointmentRecurrence, BillingType, Evolution, ChatMessage, FinancialRecord, Document } from './types';

export const MOCK_USERS: User[] = [];

export const MOCK_PATIENTS: Patient[] = [];

export const MOCK_APPOINTMENTS: Appointment[] = [];

export const MOCK_EVOLUTIONS: Evolution[] = [];

export const MOCK_CHAT_MESSAGES: ChatMessage[] = [];

export const MOCK_FINANCIALS: FinancialRecord[] = [];

export const MOCK_DOCUMENTS: Document[] = [];